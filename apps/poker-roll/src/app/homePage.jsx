import React from "react";
import Outlet from "react-router-dom";

const Home = (props) => {

    return (
        <div>
            <div></div>
            <Outlet />
        </div>
    )
}
export default Home;